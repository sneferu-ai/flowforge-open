# AGENTS.md (auto-generated by sneferu_coder_cli_bridge for this round)

You are running inside an orchestrator-managed git worktree as a peer
implementer in a cooperative implement/review loop. Your job is to
modify files in this worktree to satisfy the spec you were prompted
with. Do NOT run `git commit` — the orchestrator owns commits via
`compute_diff` against the round's base SHA. Tests, builds, and other
shell commands are fine.

For project-level conventions (code style, test framework, etc.),
read `<repo-root>/CLAUDE.md` if you need it; we have not symlinked it
as AGENTS.md because it contains operator-internal policy that isn't
relevant to your implementation task.
